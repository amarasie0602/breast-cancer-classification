services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./checkpoints:/app/checkpoints:ro
    environment:
      - CHECKPOINT_PATH=/app/checkpoints/best_mag40.pt
      - SUBTYPE_CHECKPOINT_PATH=/app/checkpoints/best_subtype.pt
